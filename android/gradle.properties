org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Works around a Gradle 9 / Kotlin 2.3 Build-Tools-API race condition where
# concurrent workers register the same incremental-cache file storage and
# crash with "Storage for [...] is already registered". Forces clean
# (non-incremental) Kotlin compilation instead of hitting that codepath.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
